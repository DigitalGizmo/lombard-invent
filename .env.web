# Web build — assets served from the museum's own server.
# TODO: replace the placeholder below with the real web asset path (keep the trailing slash).
VITE_ASSET_PATH=REPLACE_WITH_WEB_ASSET_PATH/
VITE_KIOSK=false
